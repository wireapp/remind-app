# Datasource config
quarkus.datasource.db-kind=postgresql
quarkus.datasource.jdbc.url=${DB_URL}
quarkus.datasource.username=${DB_USER}
quarkus.datasource.password=${DB_PASSWORD}
quarkus.hibernate-orm.log.sql=false

# Run Flyway migrations automatically
quarkus.flyway.migrate-at-start=true

# Quartz config to schedule programmatic jobs
quarkus.scheduler.start-mode=forced
quarkus.quartz.store-type=jdbc-cmt

# LOGGING
quarkus.log.console.json.enabled=true
quarkus.log.console.json.zone-id=UTC
quarkus.log.console.json.exception-output-type=formatted
quarkus.log.console.json.key-overrides=logger_name=logger,thread_name=thread,mdc=context,\
  stack_trace=exception
quarkus.log.console.json.excluded-keys=sequence,loggerClassName,threadId,ndc,hostName,\
  processName,processId

#METRICS
quarkus.micrometer.binder.jvm=true
quarkus.micrometer.binder.system=true
quarkus.micrometer.binder.uptime=true
quarkus.micrometer.binder.http-server.enabled=true
quarkus.micrometer.export.prometheus.enabled=true
